/node_modules
.env
.env.local
# Temporales antiguos de multer (ya no se usan ni se sirven)
/uploads
# Archivos subidos (media de regalos)
/storage
# Build de producción del frontend (scripts/build-web.js)
/web
/.web.tar.gz
